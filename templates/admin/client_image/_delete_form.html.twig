<form method="post" action="{{ path('admin_clientImage_delete', {'id': client_image.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client_image.id) }}">
    <button class="btn">Delete</button>
</form>
